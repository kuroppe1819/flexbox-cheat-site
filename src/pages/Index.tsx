import React from 'react';
import { FlexboxGroup } from '../components/FlexboxGroup';
import { PageRoot } from '../components/root/PageRoot';

const Component = () => {
    return (
        <PageRoot>
            <FlexboxGroup id="display__flex" />
        </PageRoot>
    );
};

export const Index = Component;
