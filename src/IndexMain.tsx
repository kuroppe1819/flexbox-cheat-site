import React, { ReactElement } from 'react';
import { Header } from './components/Header';

export const IndexMain = (): ReactElement => {
    return (
        <>
            <Header />
        </>
    );
};
