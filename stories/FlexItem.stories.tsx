import React, { ReactElement } from 'react';
import { FlexItem } from '../src/flex/FlexItem';

export default {
    title: 'FlexItem',
    component: FlexItem,
};

export const Base = (): ReactElement => <FlexItem />;
