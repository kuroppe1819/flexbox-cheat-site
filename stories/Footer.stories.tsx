import React, { ReactElement } from 'react';
import { Footer } from '../src/common/components/Footer';

export default {
    title: 'Footer',
    component: Footer,
};

export const Basic = (): ReactElement => <Footer />;
